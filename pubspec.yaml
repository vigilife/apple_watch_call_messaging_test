name: apple_watch_test
description: A new Flutter project.

publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  rexios_lints: ^5.0.0

flutter:

  uses-material-design: true

